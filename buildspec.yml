version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
    commands:
      - echo "Using Java version:"
      - java -version
      - mvn -version

  build:
    commands:
      - mvn clean package

artifacts:
  files:
    - target/*.war

